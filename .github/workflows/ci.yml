name: CI

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  build:
    name: 'Build and push'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [20]

    steps:
    - uses: actions/checkout@v6

    - name: Setup node | ${{ matrix.node }}
      uses: actions/setup-node@v6
      with:
        node-version: ${{ matrix.node }}
        cache: 'yarn'
    - run: yarn
    - run: yarn run test

    - name: Generate tag
      id: generate_tag
      run: |
        SHA=$(echo $GITHUB_SHA | head -c7)
        echo "sha=$SHA" >> $GITHUB_OUTPUT

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v6
      with:
        role-to-assume: 'arn:aws:iam::000637678697:role/ecr_role'
        aws-region: us-east-1

    - name: Login to AWS ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: Build docker image
      id: build-docker-image
      env:
        REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        TAG: ${{ steps.generate_tag.outputs.sha }}
      run: |
        docker build -t $REGISTRY/jaderhenryk_ci_app_ts:$TAG .
        docker push $REGISTRY/jaderhenryk_ci_app_ts:$TAG
        IMAGE=$(echo $$REGISTRY/jaderhenryk_ci_app_ts:$TAG)
        echo "image=$IMAGE" >> $GITHUB_OUTPUT

    - name: Deploy to App Runner
      uses: awslabs/amazon-app-runner-deploy@main
      with:
        service: app-ts
        image: ${{ steps.build-docker-image.outputs.image }}
        access-role-arn: arn:aws:iam::000637678697:role/app-runner-role
        region: us-east-1
        cpu: 1
        memory: 2
        port: 3000